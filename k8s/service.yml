apiVersion: v1
kind: Service
metadata:
  name: erwd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "erwd"
    port: 8080
  selector:
    app: erwd
