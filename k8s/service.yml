apiVersion: v1
kind: Service
metadata:
  name: wsrf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wsrf"
    port: 8080
  selector:
    app: wsrf
